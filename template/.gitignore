# SQLite acceleration layer (rebuilt from flat files, not source of truth)
*.db
*.db-wal
*.db-shm

# Session state (ephemeral)
.session-active
.recovering

# Marker files (machine-local state)
.github-setup-complete
